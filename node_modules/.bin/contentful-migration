#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../contentful-migration/bin/contentful-migration" "$@"
  ret=$?
else 
  node  "$basedir/../contentful-migration/bin/contentful-migration" "$@"
  ret=$?
fi
exit $ret
