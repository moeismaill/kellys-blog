import React from "react";

import { Icon } from '@types';

const CopyIcon: Icon = ({ fill = "#08080B", ...props }) => (
  <h1 style={{fontSize: '29px', color:'#08080B', ...props}}></h1>

);

export default CopyIcon;
